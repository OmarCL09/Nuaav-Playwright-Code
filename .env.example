# Copy to `.env` and adjust if you want to point the suite at a different environment.
# The suite runs fine with zero configuration — these are optional overrides.

BASE_URL=https://www.saucedemo.com

# Set to "1" to emulate the CI profile locally.
# CI=1
